{
  "name": "ironmebel",
  "version": "1.0.0",
  "description": "Базовый шаблон фронтенд-проекта (HTML, SCSS, JS) для IronMebel",
  "private": true,
  "scripts": {
    "dev:sass": "sass scss:css --watch",
    "build:sass": "sass scss:css --style=compressed"
  },
  "devDependencies": {
    "sass": "1.97.3"
  }
}

